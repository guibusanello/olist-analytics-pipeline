services:
  metabase:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - metabase-data:/metabase-data
      - /Users/guilhermealmeidabusanello/Desktop/olist-analytics-pipeline/data:/data
    environment:
      MB_DB_TYPE: h2
      MB_DB_FILE: /metabase-data/metabase.db

volumes:
  metabase-data: